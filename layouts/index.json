{{- $pages := slice -}}
{{- range where .Site.RegularPages "Section" "posts" -}}
    {{- if not .Draft -}}
        {{- $tags := slice -}}
        {{- if .Params.tags -}}
            {{- $tags = .Params.tags -}}
        {{- end -}}
        {{- $summary := "" -}}
        {{- if .Summary -}}
            {{- $summary = .Summary -}}
        {{- end -}}
        {{- $dateFormatted := .Date.Format "2006-01-02" -}}
        {{- $page := dict "title" .Title "content" .Plain "permalink" .Permalink "summary" $summary "tags" $tags "date" $dateFormatted "section" .Section -}}
        {{- $pages = $pages | append $page -}}
    {{- end -}}
{{- end -}}
{{- $pages | jsonify -}}